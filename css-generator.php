<?php

function sprite($files = array(), $go = '')
{
    $files_template = array();
    $path = '/home/wac/daisyB-repo/css_generator';


    is_dir($filename)
    {
        
    }

    foreach ($files as $file)
    {
        list($w_idth, $h_eight, $t_ype) = getimagesize($file);
    }
    
    foreach ($files_template as $file)
    {
        if ()
        {

        }

    //$handle = fopen('somefile.txt', 'r');

    //fclose($handle);
        
    }
}


//fopen
//fclose

